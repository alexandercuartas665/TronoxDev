name: ecorex-prototype

services:
  web-prototype:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecorex-web-prototype
    ports:
      # Host 8090 -> contenedor 8080 (8080 del host lo usa la app Blazor SuperAdmin).
      - "8090:8080"
    restart: unless-stopped
