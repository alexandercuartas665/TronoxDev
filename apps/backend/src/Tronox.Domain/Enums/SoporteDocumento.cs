namespace Tronox.Domain.Enums;

/// <summary>
/// Soporte del documento (RQ04). Electronico (nacido digital), Digitalizado (papel escaneado) o
/// Fisico (papel sin binario todavia, pendiente de digitalizar).
/// </summary>
public enum SoporteDocumento
{
    Electronico = 0,
    Digitalizado = 1,
    Fisico = 2
}
