namespace Ecorex.Domain.Enums;

/// <summary>Resultado de una corrida de extraccion. Toda corrida se persiste (exito o fallo).</summary>
public enum ScrapeRunStatus
{
    Success = 0,
    Failed = 1
}
